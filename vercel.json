{
  "version": 2,
  "builds": [
    {
      "src": "Dockerfile",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "output"
      }
    }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "/" },
    { "src": "/swagger/(.*)", "dest": "/swagger/$1" }
  ]
}
