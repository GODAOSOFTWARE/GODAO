{
  "version": 2,
  "builds": [
    {
      "src": "cmd/handler/handler.go",
      "use": "@vercel/go"
    }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "/cmd/handler/handler.go" },
    { "src": "/swagger/(.*)", "dest": "/swagger/$1" }
  ]
}
